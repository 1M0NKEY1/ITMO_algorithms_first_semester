#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

vector<int> func(vector<int> arr) {
    int n = arr.size();
    int d[n];
    int position[n];
    int previous[n - 1];
    int length = 0;

    position[0] = -1;
    d[0] = -1;
    for (int i = 0; i < n; i++) {
        d[i] = INT_MIN;
    }
    for (int i = 0; i < n - 1; i++) {
        j =
    }
}

int main() {

    return 0;
}
